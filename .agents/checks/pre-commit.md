# Pre-Commit Verification Checklist
- [ ] Working directory has no syntax errors.
- [ ] No temporary debug statements (`console.log`, `debugger`) are left in application logic.
- [ ] Code compiles without TS errors: run `npm run typecheck`.
- [ ] Commit message matches the conventional format (e.g. `feat(api): ...`).
