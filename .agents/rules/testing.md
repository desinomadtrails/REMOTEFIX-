# Testing Enforceable Standards
- Test API route endpoints using Hono's `app.request()` method directly.
- Mock Azure database connection pools during standalone unit tests.
- Verify status codes and JSON formats match standard specifications.
- Keep test helper files inside `tests/` or component-specific folders.
