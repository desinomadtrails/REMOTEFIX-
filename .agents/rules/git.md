# Git Enforceable Standards
- Never commit changes directly to the `main` or `release/*` branches.
- Modify files only inside feature branches created from `main`.
- Merge changes only via pull requests with a minimum of one approved review.
- Run typecheck and automated tests locally before committing changes.
